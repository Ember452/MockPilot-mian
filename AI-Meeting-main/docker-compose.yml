# ============================================================
# 本仓库的 Docker 编排已统一收敛到项目根目录：
#   d:\DEVELOP\java\MockPilot-project\docker-compose.yml
#
# 一键启动（在项目根目录执行）：
#   docker compose up -d --build
#
# 根编排包含：MySQL(自动建库建表+初始化数据)、MongoDB、Redis、
# Elasticsearch、后端(本仓库 Dockerfile)、前端(Nginx 反代 /api)。
#
# 如仅需单独构建后端镜像：
#   docker build -t mockpilot-backend .
# ============================================================
